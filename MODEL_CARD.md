# 접촉 판정 모델 카드 (ContactScorer)

이 모델은 iPad 전면 카메라의 RGB 영상으로 손끝이 얼굴 피부에 닿았는지를 추정한다. 피부에 가까이 있지만 닿지 않은 상태와 실제 접촉을 구분하는 온디바이스 CNN이며, 깊이 센서 입력은 사용하지 않는다.

고루에서는 이 판정을 얼굴의 접촉 위치 기록에 사용한다. 모델이 자외선 차단제의 존재, 도포량, 차단 효과를 직접 측정하는 것은 아니다. 이 문서는 2026년 8월 26일 기준 배포 모델의 구조, 평가 결과와 적용 범위를 설명한다. 실행 속도는 [실기기 성능](PERFORMANCE.md)에서 별도로 다룬다.

| 항목 | 내용 |
|---|---|
| 입력 | 손끝 주변 128×128 영상 패치에서 구성한 5채널 텐서 |
| 구조 | Depthwise separable convolution을 사용하는 경량 CNN |
| 출력 | 손가락별 접촉 점수(logit), 후처리를 거친 접촉/비접촉 판정 |
| 파라미터 수 | 50,209 |
| 학습 자료 | 직접 수집한 참가자 3명의 63,984개 표본 |
| 별도 평가 | 학습에 참여하지 않은 참가자 1명의 정지 접촉과 문지르기 자료 |
| 배포 | Core ML, FP16, 접촉 판정 목표 주기 15Hz |
| 지원 손가락 | 검지, 중지, 약지, 새끼손가락. 엄지는 학습 표본이 없어 제외 |

## 1. 모델 구조와 입력

### 입력과 전처리

MediaPipe로 손과 얼굴 landmark를 추정한 뒤, 판정할 손끝을 중심으로 crop한다. 손가락 마디의 방향과 길이를 기준으로 rotation/scale normalization을 적용해 손의 방향이나 카메라와의 거리가 바뀌어도 패치 배치가 유사해지도록 한다.

RGB 영상을 그대로 넣는 대신 다음 다섯 채널을 사용한다.

| Channel | 정의와 역할 |
|---|---|
| `normalizedLuma` | 패치별로 표준화한 BT.709 luma. 손끝과 피부 주변의 명암을 표현한다. |
| `lumaDelta` | 약 0.1초 전 영상을 현재 패치의 affine으로 재투영한 뒤 계산한 luma 차이. 두 시점의 영상을 같은 좌표계에서 비교한다. |
| `absoluteLumaDelta` | `lumaDelta`의 절대값. 변화 방향과 무관하게 luma 변화량을 표현한다. |
| `handMaskSoft` | 손 landmark 21점의 capsule union으로 구성한 soft mask. 손 영역과 경계를 표현한다. |
| `faceDistance` | 얼굴 landmark의 convex hull 경계까지의 signed distance. 물리적인 피부 깊이 측정값은 아니다. |

### CNN 아키텍처

Stem Conv2d 뒤에 depthwise separable convolution 블록 3개를 연결한다. 각 블록은 3×3 depthwise convolution과 1×1 pointwise convolution으로 구성하며, 공간 연산과 채널 결합을 분리해 계산량을 줄인다.

| Stage | 연산 | Output shape (C×H×W) |
|---|---|---|
| Input | 5채널 패치 | 5×128×128 |
| Stem | 3×3 Conv2d, stride 2 | 64×64×64 |
| Block 1 | 3×3 depthwise convolution, stride 2 → 1×1 pointwise convolution | 96×32×32 |
| Block 2 | 동일 | 128×16×16 |
| Block 3 | 동일 | 192×8×8 |
| Head | Global average pooling (GAP) → Linear(192, 1) | logit 1개 |

각 convolution 뒤에 BatchNorm과 ReLU를 사용한다. 사전학습 모델은 사용하지 않았다. Sigmoid로 변환한 점수는 판정에 활용하지만, 실제 접촉 확률로 보정됐음을 보장하는 값은 아니다.

## 2. 데이터와 학습

### 참가자와 데이터 구성

**성별과 연령이 서로 다른 4명이 참여했다.** 이 중 3명의 자료를 학습과 참가자 교차검증에 사용했고, 나머지 1명의 자료를 별도 평가에 사용했다. 성별과 연령 집단별 성능이나 피부톤별 성능을 평가한 것은 아니다.

| 용도 | 참가자 수 | 촬영 세션 | 표본 수 | 접촉 | 비접촉 |
|---|---:|---:|---:|---:|---:|
| 최종 모델 학습 | 3 | 65 | 63,984 | 21,068 | 42,916 |
| 별도 참가자 정지 접촉 평가 | 1 | 20 | 21,639 | 10,431 | 11,208 |
| 같은 참가자의 동작 평가 | 1 | 22 | 17,858 | 7,334 | 10,524 |

표본 수는 영상을 초당 20개 시점으로 추출하고 검수한 뒤의 수다. 서로 인접한 영상 프레임은 유사하므로 표본 수가 독립적인 경험의 수와 같지는 않다.

학습 자료는 얼굴 부위, 손가락, 머리 각도를 바꾸며 손끝을 1초 이상 대었다 떼는 정지 접촉 과제로 수집했다. 동작 평가에는 볼, 이마, 턱 문지르기, 천천히 손끝 떼기, 닿지 않고 문지르거나 스치는 동작을 포함했다. 최종 모델의 학습 자료에는 문지르기 과제가 포함되지 않았다.

검지 표본은 37,666개로 가장 많았고, 중지 13,857개, 약지 5,867개, 새끼손가락 6,594개였다. 엄지 표본은 없다.

### 라벨링과 품질 관리

참가자가 실제 접촉 중 화면의 라벨 버튼을 누르는 방식으로 접촉 구간을 기록했다. 버튼 반응 지연의 영향을 줄이기 위해 접촉 시작과 종료 전후 0.1초의 표본을 제외했다. 선행연구에서처럼 정전식 등의 센서로 접촉을 자동 측정하지는 않았다.

학습 자료 검수에서는 모호한 장면이나 랜드마크 추적 오류가 있는 표본 4,377개를 제외했으며, 접촉/비접촉 라벨을 뒤집지는 않았다. 별도로 긴 비접촉 구간에서 참가자별로 추출한 40개 구간을 라벨과 예측값을 가리고 직접 사람이 검수했을 때 모두 비접촉으로 판단됐다. 검수자는 수집 당사자 1명이었다.

### 학습 조건

| 항목 | 설정 |
|---|---|
| Loss | Focal loss, gamma=2.0. 접촉 가중치 2.0370, 비접촉 가중치 1 |
| Class weight | 학습 표본의 비접촉/접촉 비율(42,916 / 21,068) |
| Optimizer | Adam, learning rate 0.001 고정, batch size 64 |
| Epoch | 30 epochs. Best epoch을 선택하지 않고 마지막 epoch 사용 |
| Seed | 최종 학습 구성에 대해 5개 seed로 반복 |
| Data augmentation | 사용하지 않음 |
| Input normalization | 학습 데이터에서 계산한 통계 사용 |
| 환경 | Apple Silicon MPS, PyTorch 2.13.0, Python 3.13.9 |

배포할 seed는 평가 전에 정했다. 전체 자료를 사용하는 최종 학습의 모니터링 검증 표본은 학습 표본과 중복되므로, 해당 AUPRC를 일반화 성능으로 제시하지 않는다.

## 3. 실시간 판정 규칙

모델 점수는 참가자와 촬영 조건에 따라 분포가 달라질 수 있다. 이를 완화하기 위해 촬영 세션 안에서 손가락별로 점수의 평균과 표준편차를 누적하고, 표준화 점수인 z-score로 접촉을 판정한다. 정답 라벨이나 추가 학습 없이 통계만 갱신하는 방식이다.

- **접촉 시작:** z-score가 0.45 이상이고 sigmoid 점수가 0.25 이상이어야 한다.
- **접촉 유지:** z-score가 0.45 이상이면 접촉을 유지한다.
- **접촉 해제:** z-score가 0.45 미만이면 비접촉으로 판정한다.

접촉 시작에 별도의 점수 하한을 둔 것은 긴 비접촉 구간에서 작은 점수 변화가 접촉으로 잘못 판정되는 현상을 줄이기 위해서다. 이 조건은 오탐을 줄이는 대신 약한 접촉 시작을 놓칠 수 있다. 앱의 목표 판정 주기는 15Hz이며, 판정에는 median filter나 추가적인 연속 프레임 조건을 쓰지 않는다.

## 4. 평가 방법과 결과

### 평가 방법

**LOSO(leave-one-subject-out)**에서는 학습 참가자 3명 중 한 명을 검증용으로 제외하고 나머지 두 명으로 학습하는 과정을 반복했다. 같은 참가자의 프레임을 무작위로 학습과 검증에 나누지 않았다. 이 결과를 이용해 모델 구성과 판정 조건을 비교했다.

**별도 참가자 평가**는 학습에 참여하지 않은 네 번째 참가자로 수행했다. 같은 참가자의 문지르기 자료는 8월 26일 고정된 모델과 규칙으로 처음 평가했으며, 그 결과를 모델이나 판정 조건 변경에 사용하지 않았다.

아래 수치는 저장된 영상 입력과 모델 예측을 이용하여 개발에 사용된 노트북에서 검증한 결과이다.
| 지표 | 의미 |
|---|---|
| Contact event recall | 실제 접촉 이벤트 중 최소 한 시점 이상 접촉으로 검출한 이벤트의 비율 |
| Hover FP (s/min) | 평가 대상의 전체 재생 시간 1분당, 비접촉을 접촉으로 잘못 판정한 시간 |
| Rub coverage | 각 문지르기 이벤트의 실제 접촉 구간 중 검출된 시점의 비율을 구한 뒤, 이벤트별로 동일 가중 평균한 값 |
| Fragmentation / internal gap | 검출된 이벤트 안에서 예측이 몇 개 segment로 나뉘는지와 내부 미검출 공백의 길이 |

Event recall은 접촉을 한 번이라도 찾았는지를 나타낸다. 접촉 시작과 종료 시점을 정확히 맞췄거나 접촉을 계속 추적했다는 의미는 아니므로, 문지르기에는 rub coverage와 fragmentation 지표를 함께 제시한다. Rub coverage는 앱 화면의 도포 영역 비율과 다른 지표다.

Hover FP의 재생 시간에는 접촉과 비접촉 시간이 모두 포함된다. 비접촉 시간만을 분모로 삼은 오탐률과는 다르다.

### 정지 접촉 (학습에 참여하지 않은 1명)

2026년 8월 23일 평가이며 z-score 0.45 기준을 사용했다. 이후 추가한 접촉 시작 점수 하한은 적용하지 않았다.

| 지표 | 배포 모델 | 5개 seed 평균 ± 표준편차 |
|---|---:|---:|
| Contact event recall | 91.74% | 90.28% ± 2.24%p |
| Hover FP (s/min) | 1.2393s/min | 1.2975 ± 0.2827s/min |

### 문지르기 (같은 별도 참가자)

2026년 8월 26일 평가이며, 접촉 시작 점수 하한까지 포함한 **최종 판정 규칙**을 15Hz로 재생해 평가했다. 문지르기 이벤트는 51개다.

| 지표 | 배포 모델 | 5개 seed 평균 ± 표준편차 |
|---|---:|---:|
| Contact event recall | 98.04% (50/51) | 96.86% ± 2.24%p |
| Rub coverage | 42.69% | 38.92% ± 3.14%p |
| Segments / detected event | 8.32개 | 8.4209 ± 0.3842개 |
| Max internal gap (median) | 0.7333초 | 0.7667 ± 0.0333초 |
| Rub-task hover FP (s/min) | 1.2534s/min | 1.0637 ± 0.4421s/min |

대부분의 문지르기 이벤트를 한 번 이상 검출했지만, 접촉 구간 전체를 지속해서 검출하지는 못했다. 문지르기 오탐 시간의 분모는 문지르기 과제의 전체 평가 재생 시간이다. 그중 실제 비접촉인 시점의 오탐을 집계했으며, 정지 접촉이나 비접촉 전용 과제와 조건이 같지 않다.

## 5. 한계와 개선 방향

### 5.1 인접 손가락의 영향

다른 손가락이 입력 패치에 겹치면 접촉을 놓치거나 비접촉을 접촉으로 잘못 판정할 수 있다. 8월 25일 분석에서는 일부 참가자의 접촉 검출률이 인접 손가락이 가까워질수록 낮아졌다. 이러한 장면은 데이터에 적게 포함돼 있다. 손가락이 붙거나 겹친 상태의 접촉과 비접촉 학습 자료를 보충하면 이 취약성을 줄이고 성능을 높일 가능성이 있다.

### 5.2 참가자와 데이터 다양성

성별과 연령이 서로 다른 4명이 참여했지만, 학습 3명과 별도 평가 1명만으로 다양한 사용자를 대표하기는 어렵다. 연속 영상의 많은 프레임도 참가자 수의 부족을 대신하지 못한다. 참가자 수를 늘리고 손 모양, 동작 습관, 피부톤과 조명 조건이 다양한 학습 자료를 보충하면 일반화 성능을 개선할 가능성이 있다. 더 많은 참가자 수와 샘플이 더 좋은 성능을 만들 수 있는지에 대해서는 아직 확증하지 않았다.

### 5.3 문지르기 중 접촉 검출

최종 모델은 정지 접촉 자료로 학습했으며, 문지르기에서는 event recall에 비해 rub coverage가 낮았다. 빠른 이동과 여러 손가락의 겹침을 포함한 접촉을 지속해서 추적하려면 추가 데이터와 모델 검토가 필요하다. 기존의 문지르기 자료 편입 실험에서는 채택할 개선을 얻지 못했으므로, 자료를 단순히 추가하는 것만으로 해결된다고 볼 수는 없다.

## 6. 선행연구와 평가 기준

얼굴 접촉 인식과 RGB 영상 기반 피부 접촉 연구는 존재한다. 예를 들어 PAVIS의 연구는 사회적 상호작용 영상의 얼굴 접촉 여부를 다루며, 프레임별 접촉 라벨 및 손과 얼굴 키포인트를 제공한다. 얼굴을 문지르는 동안 특정 손끝의 접촉이 계속 유지되는지 평가하는 과제와는 구분된다. [Analysis of Face-Touching Behavior, ICMI 2020](https://github.com/IIT-PAVIS/Face-Touching-Behavior)

EgoTouch는 팔과 손 피부의 접촉을 RGB 영상으로 인식한다. 성별과 피부톤 등이 다양한 15명에 대해 참가자 독립 평가를 수행했지만, 드래그 동작은 정답 센서를 부착한 테이프가 떨어지는 문제로 정식 사용자 평가에서 제외했다. [EgoTouch, UIST 2024, §4–6](https://arxiv.org/html/2509.01786v1)

위 문헌을 검토한 범위에서는, 얼굴 문지르기의 손끝 접촉 지속 검출에 직접 적용할 수 있는 공통 벤치마크나 합격 기준을 확인하지 못했다. 이는 관련 연구가 없다는 주장이나 체계적 문헌조사 결과는 아니다.

