# 실기기 성능

GoruShowcase를 iPad에서 계측한 처리 시간과 실행 조건을 정리한다. 접촉 판정의 정확도는 [모델카드](MODEL_CARD.md)에서 별도로 다룬다. 아래 수치는 특정 기기와 측정 구간의 결과이며 모든 실행 조건을 보장하지 않는다.

## 1. 측정 환경

모든 실기기 계측은 iPad Pro 11형 4세대(`iPad14,3`, Apple M2) 한 대에서 수행했다. 다른 기기에서의 성능은 검증하지 않았다.

## 2. 2026년 9월 9일 5분 연속 계측 (최신)

아래 수치는 시연용 빌드(Release, 진단 계측 포함)를 설치하고 5분간 연속 실행해 얻었다. 번들 모델은 최종 앱과 같은 후속 모델 D3다. 배포 모델 C0와의 차이는 [모델카드 7절](MODEL_CARD.md#7-후속-모델d3과-시연-앱의-번들-모델)에 적었고, 구조와 파라미터 수 50,209개는 두 모델이 같다. 접촉 CNN을 계속 실행한 상태에서 측정했으며, 5분 동안 CNN cycle 4,165회를 처리해 평균 약 13.9Hz였다(목표 15Hz).

| 항목 | 측정값 | 비고 |
|---|---:|---|
| CNN cycle 평균 / 최대 | 7.35ms / 17.1ms | 보고서에는 평균 7.4ms로 반올림 |
| Core ML 추론 평균 | 0.73ms | CNN 추론 단독 시간 |
| 캡처→접촉 점수 평균 | 43.0ms | 입력 프레임 캡처부터 접촉 점수 산출까지 |
| 접촉 시작 캡처→화면 표시 평균 / 최대 | 81.0ms / 117.5ms | 571건. 보고서에는 81ms / 118ms로 반올림 |
| 카메라 | 60fps, 프레임 드랍 0 | |
| 발열 상태 최대 | nominal | |
| 메모리 최대 | 1,088.4MB | 보고서에는 1,088MB로 반올림 |
| Hand landmark 처리 빈도 | 39.8~48.4Hz | 1분 구간별 범위 |

"접촉 시작 캡처→화면 표시"는 접촉 시작으로 판정된 입력 프레임의 캡처 시각부터 그 판정이 화면에 그려진 프레임의 표시 시각까지의 시간이다. 손이 물리적으로 피부에 닿은 순간부터 재는 값이 아니라, 접촉 시작 확인 게이트를 통과한 판정이 렌더링에 반영되기까지의 지연이다.

## 3. CNN cycle (n=763, 2026년 8월 24일 측정)

접촉 판정의 목표 주기는 15Hz이며 한 사이클의 시간 예산은 66.7ms다.

| 항목 | 측정 범위 | 평균 |
|---|---|---:|
| Core ML 추론 | `MLModel.prediction` 호출 시간 | 0.9ms |
| 전처리 | 사이클의 손가락 패치 구성에 걸린 시간 | 8.9ms |
| CNN cycle | 전처리와 추론을 포함한 사이클 처리 시간 | **9.5ms** |
| 캡처→접촉 점수 | 입력 프레임 캡처부터 접촉 점수 산출까지의 시간 | **39.6ms** |

평균 CNN cycle 시간 9.5ms는 목표 예산의 약 14%다. 2026년 9월 9일 측정의 7.35ms는 같은 예산의 약 11%다.

이 측정에서는 Core ML 추론보다 전처리에 더 많은 시간이 들었다. 실제 연산이 ANE, GPU, CPU 중 어디에서 수행됐는지는 확인하지 않았다.

캡처→접촉 점수 시간에는 CNN 처리 외에 landmark 결과 대기와 스케줄링 등이 포함된다.

## 4. MediaPipe와 결합한 상태 (2026년 8월 24일 측정)

같은 측정 구간에서 MediaPipe face/hand landmark 추론과 접촉 CNN을 함께 실행했다.

| 항목 | 평균 또는 관측값 |
|---|---:|
| 카메라 FPS | 60.01fps |
| 카메라 프레임 드랍 | 0 |
| Face landmark 추론 | 8.28ms |
| Hand landmark 추론 | 12.08ms |
| Face landmark 처리 빈도 | 58.73Hz |
| Hand landmark 처리 빈도 | 52.11Hz |
| 렌더링 GPU 완료 지연 | 15.41ms |

렌더링은 비동기 결과를 사용한다. 위 15.41ms는 렌더링이 시작된 시각부터 렌더링 GPU 작업 완료까지의 시간이다.

## 5. 장시간 연속 실행 (2026년 8월 4일 측정)

2026년 8월 4일의 19분 30초 연속 실행 기록이다. 코칭 화면을 유지하며 간헐적으로 손을 얼굴에 올렸다. 2026년 9월 9일 5분 측정(2절)은 접촉 CNN을 계속 실행한 상태다.

| 구간 | Face landmark inference | Hand landmark inference |
|---|---:|---:|
| 0–2분 | 9.2ms | 10.2ms |
| 8–10분 | 9.3ms | 10.1ms |
| 17–19분 | 9.1ms | 10.5ms |

이 조건에서는 시간이 지남에 따라 MediaPipe 추론 시간이 크게 증가하지 않았다.
